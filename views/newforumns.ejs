<%for(var i=Comm.length-1;i>=0;i--){%>
    <a href="/blank/<%=Comm[i]._id%>">
<button class="btn btn-light btn-sm"value="<%=Comm[i]._id%>" name="btn" onclick="this.form.submit()">
<%=Comm[i].subject%></a></button>
    <button class="btn btn-light btn-sm" id="<%=Comm[i]._id%>" value="<%=Comm[i].subject%>">Join</button>
    <p>Created By
        <%=Comm[i].creator.name%>
    </p>
    <br>
    <script>
        $(document).ready(function() {
            var button = "<%=user%>"
            $("#<%=Comm[i]._id%>").on('click', function() {
                var value = {
                    button: "<%=Comm[i].subject%>"
                }
                console.log(button);
                $("#<%=Comm[i]._id%>").html("Joined");
                $.ajax({
                    type: "POST",
                    url: "/requestjoin/<%=Comm[i]._id%>",
                    data: {
                        button: button
                    },
                    success: function(data) {
                        console.log("success");

                    }
                });
            });
        });
    </script>
    <%};%>
        </div>
        <a href="/topics"> Click this link and explore </a>